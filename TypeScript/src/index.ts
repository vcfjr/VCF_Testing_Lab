//== BASICS ==//

/**
 * Add two numbers
 * @function addNumbers Input two numbers 
 * @param a first number
 * @param b second number
 * @returns Sum of a and b
 */

export function addNumbers(a: number, b: number) : number {
    return a + b;
}