/**
 * (1) x is a string because we have initiated it with string
 */

let x = "Hello world"

/**
 * (2) reassignment is fine
 */

x = "Hello mars"

/**
 * (3) but if we try to change type 
 */

// x =42 // ERROR